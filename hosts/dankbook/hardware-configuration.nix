# PLACEHOLDER — replace with dankbook's real hardware config.
# On dankbook, run from the repo root:
#   sudo nixos-generate-config --show-hardware-config > hosts/dankbook/hardware-configuration.nix
# Then delete the old untracked hardware-configuration.nix at the repo root,
# git add this file, and rebuild.
throw "hosts/dankbook/hardware-configuration.nix is a placeholder — copy dankbook's real hardware config here (see comments in this file)"
